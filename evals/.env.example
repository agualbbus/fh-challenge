# Eval harness secrets. Copy to evals/.env (gitignored) and fill in.
# Loaded by evals/env_config.py at module import.
#
# Any env var named by `api_key_env` in config.yaml can be set here.

# WATCHTOWER_API_KEY=replace-me
